# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/values.schema.json
defaultPodOptions:
  hostNetwork: true
  securityContext:
    fsGroup: 1000
    fsGroupChangePolicy: OnRootMismatch
    runAsGroup: 1000
    runAsUser: 1000

service:
  main:
    type: ClusterIP
    controller: main
    ports:
      http:
        port: &esphome-port 6052

ingress:
  tailscale:
    className: tailscale
    enabled: true
    hosts:
      - host: &host nl-esphome.tail2ff90.ts.net
        paths:
          - path: "/"
            pathType: Prefix
            service:
              identifier: main
              port: http
    tls:
      - hosts: [*host]

controllers:
  main:
    type: deployment
    replicas: 1
    annotations:
      "reloader.stakater.com/auto": "true"
    containers:
      main:
        env:
          PGID: 1000
          PGUID: 1000
          TZ: Europe/Amsterdam
        image:
          repository: ghcr.io/home-operations/esphome
          tag: 2026.3.0@sha256:aa94abc9a1234a4e5de6b54b87687ba98b333f45dbb6dda2058ff16dab576855
        probes:
          liveness: { enabled: false }
          readiness: { enabled: false }
          startup: { enabled: false }
        resources:
          requests:
            cpu: 5m
            memory: 512Mi
          limits:
            memory: 3072Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          # capabilities:
          #   drop:
          #     - ALL

persistence:
  config:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: esphome-config
    globalMounts: [{ path: /config }]
  tmpfs:
    enabled: true
    type: emptyDir
    globalMounts:
      - { path: /cache, subPath: cache }
      - { path: /tmp, subPath: esphome-tmp }
