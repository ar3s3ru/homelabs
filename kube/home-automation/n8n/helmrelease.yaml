---
# n8n workflow automation, queue-mode, Tailscale-only.
# Chart ref: https://github.com/8gears/n8n-helm-chart
apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: n8n
  namespace: &namespace home-automation
spec:
  chart: oci://8gears.container-registry.com/library/n8n
  version: 2.0.1
  targetNamespace: *namespace
  valuesContent: |-
    image:
      repository: n8nio/n8n
      tag: 2.16.1@sha256:ad20607cdd24bac004ec44804b6b8ded9a2fbf92ed46c4496bf007762c883af2
      pullPolicy: IfNotPresent

    # Global ingress: Tailscale-only, served by the main pod.
    ingress:
      enabled: true
      className: tailscale
      annotations: {}
      hosts:
        - host: &host nl-n8n.tail2ff90.ts.net
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - *host

    # # # # # # # # # # # # # # # #
    # Main (editor UI + webhooks)
    # # # # # # # # # # # # # # # #
    main:
      # Values transformed 1:1 into ENV variables (see chart README).
      config:
        generic:
          timezone: Europe/Amsterdam
        executions_mode: queue
        n8n:
          editor_base_url: &url https://nl-n8n.tail2ff90.ts.net/
          # Trust the Tailscale ingress proxy for client IP resolution.
          proxy_hops: 1
        # Webhooks processed by the main pod (no dedicated webhook deployment).
        webhook_url: *url
        db:
          type: postgresdb
          postgresdb:
            host: n8n-cnpg-cluster-rw.home-automation.svc.cluster.local
            port: 5432
            database: n8n
            pool:
              size: 5
        queue:
          health:
            check:
              active: true
          bull:
            redis:
              host: n8n-redis.home-automation.svc.cluster.local
              port: 6379

      extraEnv: &extraEnv
        DB_POSTGRESDB_USER:
          valueFrom:
            secretKeyRef:
              name: n8n-cnpg-cluster-app
              key: username
        DB_POSTGRESDB_PASSWORD:
          valueFrom:
            secretKeyRef:
              name: n8n-cnpg-cluster-app
              key: password
        N8N_ENCRYPTION_KEY:
          valueFrom:
            secretKeyRef:
              name: n8n-secrets
              key: N8N_ENCRYPTION_KEY

      persistence:
        enabled: true
        type: existing
        existingClaim: n8n-data-v1

      replicaCount: 1
      deploymentStrategy:
        type: Recreate

      podAnnotations:
        reloader.stakater.com/auto: "true"

      podSecurityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

      service:
        type: ClusterIP
        port: 5678

      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 2Gi

      livenessProbe:
        httpGet:
          path: /healthz
          port: http
        initialDelaySeconds: 30
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 6
      readinessProbe:
        httpGet:
          path: /healthz
          port: http
        initialDelaySeconds: 10
        periodSeconds: 15
        timeoutSeconds: 5
        failureThreshold: 6

    # # # # # # # # # # # # # # # #
    # Workers (queue consumers)
    # # # # # # # # # # # # # # # #
    worker:
      enabled: true
      replicaCount: 2
      concurrency: 10

      # Workers share DB + Redis + encryption key with the main pod.
      extraEnv: *extraEnv

      # Workers don't need the main PVC; they coordinate via Redis + Postgres.
      persistence:
        enabled: false

      deploymentStrategy:
        type: RollingUpdate

      podAnnotations:
        reloader.stakater.com/auto: "true"

      podSecurityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

      resources:
        requests:
          cpu: 100m
          memory: 384Mi
        limits:
          memory: 1Gi

      # The worker pods expose /healthz on the chart's default http port.
      livenessProbe:
        httpGet:
          path: /healthz
          port: http
        initialDelaySeconds: 30
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 6
      readinessProbe:
        httpGet:
          path: /healthz
          port: http
        initialDelaySeconds: 10
        periodSeconds: 15
        timeoutSeconds: 5
        failureThreshold: 6

    # Webhook handled by the main pod - dedicated webhook deployment disabled.
    webhook:
      enabled: false

    # Internal Valkey bundled with the chart is disabled: we use the
    # ot-container-kit Redis operator deployment instead (see helmrelease-redis.yaml).
    valkey:
      enabled: false
